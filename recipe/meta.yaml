{% set name = "lumicks.pylake" %}
{% set version = "0.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 61e11305b74f2135cc0f22bd58e5bfe49f27293008711c62282d50416d675ec3

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6,<4
    - numpy >=1.14,<2
    - matplotlib-base >=2.2
    - h5py >=3.0,<4
    - pytest >=3.5
    - scipy >=1.1,<2
    - tifffile >=0.15,<1.0
    - tabulate ==0.8.6
    - opencv >=3.0
    - ipywidgets >=7.0.0
    - cachetools >=3.1

test:
  imports:
    - lumicks.pylake
    - lumicks.pylake.tests
  requires:
    - pytest
  commands:
    - pytest --pyargs lumicks.pylake

about:
  home: https://github.com/lumicks/pylake
  license: Apache-2.0
  license_file: license.md
  summary: Data analysis tools for Lumicks Bluelake
  dev_url: https://github.com/lumicks/pylake

extra:
  recipe-maintainers:
    - dean0x7d
    - onnodb
    - JoepVanlier
